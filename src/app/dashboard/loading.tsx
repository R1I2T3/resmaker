import React from "react";
import { Loader } from "@/client/components/ui/Loader";
const Loading = () => {
  return (
    <div>
      <Loader size={32} color="orange-500" />
    </div>
  );
};

export default Loading;
